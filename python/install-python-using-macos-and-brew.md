# Python install using macOS and brew

Run:

```sh
brew install python
```

If you want Tk:

```sh
brew install python-tk tcl-tk
```

If you want various dependency managers:

```sh
brew install pipx poetry pyenv pyenv-virtualenv pyenv-virtualenvwrapper 
```
